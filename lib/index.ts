/**
 * Lib Directory
 * 
 * This directory contains third-party library configurations and wrappers.
 * 
 * Structure:
 * - api-client/   - API client setup (axios, fetch wrapper)
 * - storage/      - Storage library setup (AsyncStorage wrapper)
 * - analytics/    - Analytics library setup
 * 
 * Example usage:
 * ```typescript
 * import { apiClient } from '@/lib';
 * const response = await apiClient.get('/users');
 * ```
 */

// Export library configurations here
// Example: export { apiClient } from './api-client/axios-client';
// Example: export { storage } from './storage/storage-wrapper';

